apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: argo-cd-letsencrypt
  namespace: argo-cd
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: reiniernel89@gmail.com
    privateKeySecretRef:
      name: argo-cd-letsencrypt
    solvers:
    - http01:
        ingress:
          class: nginx
          podTemplate:
            spec:
              nodeSelector:
                "kubernetes.io/os": linux